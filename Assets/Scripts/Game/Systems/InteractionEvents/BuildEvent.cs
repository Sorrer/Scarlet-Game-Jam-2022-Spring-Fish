using UnityEngine;

namespace Game.Systems.InteractionEvents
{
    public class BuildEvent : MonoBehaviour
    {
        //Shows ghost of object
            //Rendering per object
            
            
        //Each build spot has the ghost image already there tied to a building ID to search for
        //Building id enabled so you can build
        //Plop foundation of building based on bounding box
        //When transition activates, replace with actual building
    }
}